package src;

import java.util.ArrayList;
import java.util.List;

public class players_with_zero_or_one_loses {
    public void findWinners(int[][] matches) {
        ArrayList<List<Integer>> answer = new ArrayList<List<Integer>>();
        ArrayList<Integer> winner = new ArrayList<Integer>();
        ArrayList<Integer> loser = new ArrayList<Integer>();
        boolean stat = true;
        for(int i=0; i< matches.length;i++)
        {
            if (matches[i][0]-matches[i][1] != 0 || matches[i][0] != 1){
             
            }
        }
        }
    }
